{
  "name": "antigravity-marp-slides",
  "version": "1.0.0",
  "description": "Antigravityを活用したMarpスライド生成システム",
  "main": "index.js",
  "scripts": {
    "generate-html": "scripts/generate_slides.sh --format html --output presentation",
    "generate-pdf": "scripts/generate_slides.sh --format pdf --output presentation",
    "generate-pptx": "scripts/generate_slides.sh --format pptx --output presentation",
    "generate-pptx-editable": "scripts/generate_slides.sh --format pptx --output presentation --editable",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "marp",
    "slides",
    "markdown",
    "antigravity"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@marp-team/marp-cli": "^4.1.2"
  }
}